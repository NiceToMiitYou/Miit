'use strict';

var QuizApp = React.createClass({
    render: function() {
        return (
            <span className="miit-component quiz-app">
            </span>
        );
    }
});

module.exports = QuizApp;
