'use strict';

var ChatConstants = {
    ActionTypes: KeyMirror({
        // Change Chat Actions
        REFRESH_CHATROOMS: null,
        ADD_MESSAGE: null,
        OLD_MESSAGES: null
    })
};

module.exports = ChatConstants;